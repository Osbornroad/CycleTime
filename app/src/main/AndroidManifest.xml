<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.osbornroad.cycletime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_cycletime"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
<!--        <activity
            android:name=".StopWatchActivity"
            android:launchMode="singleTop"></activity>
        <activity
            android:name=".EmployeeChooseActivity"
            android:label="@string/choose_employee"
            android:parentActivityName=".StopWatchActivity" />
        <activity
            android:name=".ProcessChooseActivity"
            android:label="@string/choose_process"
            android:parentActivityName=".StopWatchActivity" />
        <activity
            android:name=".MachineChooseActivity"
            android:label="@string/choose_machine"
            android:parentActivityName=".StopWatchActivity" />
        <activity
            android:name=".PartChooseActivity"
            android:label="@string/choose_part"
            android:parentActivityName=".StopWatchActivity" />-->
        <activity
            android:name=".ResultMeasurementActivity"
            android:label="@string/stopwatch_result_title"
            android:parentActivityName=".MainActivity" />
<!--        <activity android:name=".CoreActivity">

        </activity>-->
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>